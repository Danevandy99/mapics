export interface UserSettings {
  userId: string;
  email: string;
  displayName: string;
  photoURL: string;
  emailVerified: boolean;
  radius: number;
  username: string;
  bio: string;
  notifications: boolean;
}
